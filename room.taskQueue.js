/*
build a task queue, rather than having defined roles
 */

module.exports = {
    run: function(roomName){
        const tasks = [];
        tasks.push('harvest');
        tasks.push('upgrade');
        tasks.push('harvest');
        
        
    }
};